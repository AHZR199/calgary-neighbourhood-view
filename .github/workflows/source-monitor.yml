name: check source updates
on:
  schedule:
    - cron: '15 14 * * *'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  source-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Check source availability and flag updates for review
        run: node scripts/monitor-sources.mjs
      - name: Save the source check report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: official-source-review
          path: work/source-monitor/report.json
          if-no-files-found: error
